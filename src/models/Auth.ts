export interface IUser {
	accessToken: string;
	displayName: string;
	email: string;
	username: string;
	emailVerified: boolean;
	isAnonymous: boolean;
	phoneNumber: string;
	photoURL: string;
	uid: string;
}